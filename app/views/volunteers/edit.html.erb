<div class='pb-2'>
  <span class='h3'>Editar <%= @volunteer.name %></span>
</div>
<hr />

<%= render 'shared/error_messages', resource: @volunteer %>

<%= form_for(@volunteer, url: volunteer_path(@volunteer)) do |f| %>
  <%= render 'form', f: f %>
<% end %>
