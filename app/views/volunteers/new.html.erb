<div class='pb-2'>
  <span class='h3'>Novo Voluntário</span>
</div>
<hr />

<%= render 'shared/error_messages', resource: @volunteer %>

<%= form_for(@volunteer, url: volunteers_path) do |f| %>
  <%= render 'form', f: f %>
<% end %>
